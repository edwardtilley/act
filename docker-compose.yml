services:
  libretranslate:
    image: libretranslate/libretranslate:latest
    container_name: libretranslate
    restart: unless-stopped
    ports:
      - "5030:5000"
    environment:
      - LT_LOAD_ONLY=en,fr
      - LT_HOST=0.0.0.0
      - LT_PORT=5000
    mem_limit: 2g
